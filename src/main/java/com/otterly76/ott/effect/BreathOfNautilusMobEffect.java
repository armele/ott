package com.otterly76.ott.effect;

import net.minecraft.world.effect.MobEffect;
import net.minecraft.world.effect.MobEffectCategory;

/** Prevents the rider's oxygen bar from depleting while mounted on a nautilus. */
public class BreathOfNautilusMobEffect extends MobEffect {
    public BreathOfNautilusMobEffect(MobEffectCategory category, int color) {
        super(category, color);
    }
}
